name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: self-hosted
    steps:
      - name: Print greeting
        run: echo "Hello world"
      - name: Print hostname and IP
        run: hostname && hostname -I
